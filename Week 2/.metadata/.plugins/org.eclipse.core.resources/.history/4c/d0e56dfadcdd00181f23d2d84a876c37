package com.revature.util;

public class ConnectionFactory {
	/*
	 * Conections are the vital tool in JDBC
	 * We use the ConnectionFactory to establish a connection
	 * with the database
	 * 
	 * This will use a lazy singleton design pattern to retun the same single connection factory
	 * instance each time one is requested so that we can monitor the amount of connections that
	 * our connection factory generates
	 * 
	 * In order to establish a connection, we need four things
	 * 	driver, db location, db username, db password
	 */
	

}
